# ========================================================
# SIIEC - PRODUCTION ENVIRONMENT TEMPLATE
# ========================================================

# 1. SERVER CONFIGURATION
PORT=3001
NODE_ENV=production
# Add your production domains here (comma-separated)
ALLOWED_ORIGINS=https://denunzia.org,https://www.denunzia.org

# 2. DATABASE (PostgreSQL / Supabase)
# If using Supabase, get this from Project Settings > Database
DATABASE_URL=postgres://postgres.[PROJECT_ID]:[PASSWORD]@[HOST]:5432/postgres
DB_HOST=
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=

# 3. SECURITY & CRYPTOGRAPHY
JWT_SECRET=generate_a_long_random_string_here
# Secret for internal data hashing
ENCRYPTION_SECRET=generate_another_random_32_byte_string

# RSA Private Key for Server-Side Decryption
# You can paste the whole PEM content here (replace newlines with \n)
PRIVATE_KEY_PEM="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
# Or provide a path if the file is present in the container
PRIVATE_KEY_PATH=./keys/private_key.pem

# 4. AI INTEGRATION (GEMINI)
# Get your API Key from https://aistudio.google.com/
GEMINI_API_KEY=

# 5. STORAGE / IPFS (PINATA)
# Get your JWT from https://app.pinata.cloud/
PINATA_JWT=
PINATA_GATEWAY=https://gateway.pinata.cloud

# 6. FILE LIMITS
# 50MB in bytes
MAX_PAYLOAD_SIZE=52428800
